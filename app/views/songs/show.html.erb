<h1><%= link_to @song.title, song_path(@song) %></h1>
<%if @song.artist%><p><%=display_artist(@song)%></p>
<%else%><p><%= link_to 'Add Artist', edit_song_path%></p>
<%= link_to "All Songs", songs_path %><%end%>
