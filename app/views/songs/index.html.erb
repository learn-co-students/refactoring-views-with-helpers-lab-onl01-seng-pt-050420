<ul>
<% @songs.each do |song| %>
  <li><%= link_to song.title, song_path(song) %></li>
  <%if song.artist%><p><%=display_artist(song)%></p>
<%else%><p><%= link_to 'Add Artist', edit_song_path(song)%></p>
<% end %><%end%>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
